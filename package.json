{
  "name": "swimbuddy-pro",
  "version": "1.2.0",
  "description": "SwimBuddy Pro - Swimming Performance Analytics",
  "scripts": {
    "install-deps": "cd frontend && npm install && cd ../backend && pip install -r requirements.txt",
    "build": "cd frontend && npm install && npm run build && mkdir -p ../backend/static && cp -r dist/* ../backend/static/ || (mkdir ..\\backend\\static & xcopy /E /I dist\\* ..\\backend\\static\\)",
    "start": "cd backend && uvicorn app.main:app --host 0.0.0.0 --port $PORT",
    "start-backend": "cd backend && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
  },
  "engines": {
    "node": "18.x"
  },
  "dependencies": {
    "express": "^4.21.2",
    "serve": "^14.2.0"
  }
}
