{
  "name": "swimbuddy-pro",
  "version": "1.2.0",
  "description": "SwimBuddy Pro - Swimming Performance Analytics",
  "scripts": {
    "install-deps": "cd frontend && npm install",
    "build": "cd frontend && npm install && npm run build",
    "start": "node server.js",
    "start-backend": "cd backend && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
  },
  "engines": {
    "node": "18.x"
  },
  "dependencies": {
    "express": "^4.21.2",
    "serve": "^14.2.0"
  }
}
